---
title: Pigeon
description: A local-first kanban board with MCP integration. Pigeon carries context between AI coding sessions — endSession releases it, briefMe catches it.
template: splash
---

import Hero from "../../components/Hero.astro";
import SessionLoop from "../../components/SessionLoop.astro";
import ScreenshotCallout from "../../components/ScreenshotCallout.astro";
import SectionHeader from "../../components/SectionHeader.astro";
import FeatureGrid from "../../components/FeatureGrid.astro";
import NextRead from "../../components/NextRead.astro";
import boardOverview from "../../assets/screenshots/board-overview.png";
import cardDetail from "../../assets/screenshots/card-detail.png";

<div class="landing">

<Hero
	eyebrow="Pigeon // local-first MCP kanban"
	titleHtml={`A kanban board that <em>carries context</em><br /> between AI coding sessions.`}
	tagline="You see a board. The agent reads and writes the same board through MCP. Nothing leaves your machine."
	actions={[
		{
			text: "60-second install",
			href: "./quickstart/",
			variant: "primary",
			hint: "↵ start here",
		},
		{
			text: "Why local-first?",
			href: "./why/",
			variant: "secondary",
		},
	]}
	statusItems={[
		{ label: "Storage", value: "SQLite, on disk" },
		{ label: "Network", value: "None" },
		{ label: "Surface", value: "MCP + Web UI" },
		{ label: "License", value: "Open source" },
	]}
>
	<SessionLoop slot="aside" />
</Hero>

---

<SectionHeader
	num="01"
	eyebrow="The problem"
	title="Context evaporates at the end of every chat."
	sub="You open a new session and re-explain what was planned, what's done, what was decided, what's stuck. Pigeon catches that context for you — and hands it to the next agent."
/>

<FeatureGrid items={[
	{
		tag: "01",
		title: "You see a board",
		body: "Cards, columns, priorities, checklists, comments, activity. Drag-and-drop in your browser. Everything the agent does shows up live.",
	},
	{
		tag: "02",
		title: "The agent sees structure",
		body: "Essential MCP tools are always on. Extended tools are discoverable on demand. Context footprint stays small.",
	},
	{
		tag: "03",
		title: "Sessions hand off cleanly",
		body: "endSession writes a handoff. The next chat calls briefMe and resumes exactly where the last one stopped.",
	},
	{
		tag: "04",
		title: "Local-first by design",
		body: "SQLite file on disk. No accounts, no cloud sync, no server to babysit. You own the data and the history.",
	},
]} />

---

<SectionHeader
	num="02"
	eyebrow="Surface"
	title="The board, annotated."
	sub="Five columns mark state; cards are units of work; priority rides as a colored stripe; tags filter; numbers stay stable across sessions."
/>

<ScreenshotCallout
	src={boardOverview}
	alt="Pigeon board view with Parking Lot, Backlog, Up Next, In Progress, and Done columns. Cards show priority-colored stripes, tags, and stable card numbers."
	caption="FIG.02 — Board view"
	subcaption="Tutorial board, fresh install"
	callouts={[
		{
			n: "01",
			x: 4,
			y: 12,
			label: "Columns = state",
			prose: "Parking Lot, Backlog, Up Next, In Progress, Done. The agent moves cards across columns instead of inventing new statuses.",
		},
		{
			n: "02",
			x: 28,
			y: 32,
			label: "Card = unit of work",
			prose: "One concern per card. Title, priority stripe, tags, stable #N number. Everything the agent reasons over has an id.",
		},
		{
			n: "03",
			x: 22,
			y: 62,
			label: "Priority stripe",
			prose: "Color-coded left edge. Critical, high, medium, low. Quick visual ranking without opening the card.",
		},
		{
			n: "04",
			x: 78,
			y: 82,
			label: "Tags + counters",
			prose: "Tags filter the board (tutorial, mcp, advanced). Comment and checklist counters surface card depth at a glance.",
			align: "left",
		},
	]}
/>

<ScreenshotCallout
	src={cardDetail}
	alt="Card detail panel showing description, tags, checklist, and comments for card #12 — Add Comments & Collaborate."
	caption="FIG.03 — Card detail"
	subcaption="The single screen where humans and agents converge"
	callouts={[
		{
			n: "01",
			x: 65,
			y: 6,
			label: "Stable #N",
			prose: "Card numbers never change, even after deletion. Agents reference cards by #12 across sessions and the link still resolves.",
			align: "left",
		},
		{
			n: "02",
			x: 70,
			y: 32,
			label: "Description = scope",
			prose: "What, why it matters, try-it instructions. The agent reads this on briefMe; the human edits it during planning.",
			align: "left",
		},
		{
			n: "03",
			x: 70,
			y: 76,
			label: "Checklist = sub-state",
			prose: "Lightweight subtasks. Crossing items off is a legitimate end-of-session save point — no need to split a card.",
			align: "left",
		},
		{
			n: "04",
			x: 70,
			y: 95,
			label: "Comments = guidance",
			prose: "Human nudges and agent observations. Surfaced to the next session via getCardContext. The audit trail makes clear who said what.",
			align: "left",
		},
	]}
/>

---

<SectionHeader
	num="03"
	eyebrow="The metaphor"
	title="The pigeon carries the message."
	sub="endSession releases a handoff. The wire is your local disk, not a network. briefMe catches the same payload at the next session — fully in-context, in seconds."
/>

A handoff is a SQLite row. A row knows the project, the active card, the
recent decisions, the open threads. It does not know your network — there
isn't one to know. The next session's first move is `briefMe`; what comes
back is the handoff plus the current board pulse plus any open blockers.

> **Built for two readers.** A human who wants a board. An agent that
> wants structure. The same SQLite file serves both, all the time, with
> no sync layer between them.

---

<SectionHeader
	num="04"
	eyebrow="Continue"
	title="What to read next."
	sub="Two paths in. Surface readers go to the quickstart. Depth readers go to the rationale."
/>

<NextRead links={[
	{
		href: "./quickstart/",
		label: "Quickstart",
		desc: "Clone, install, connect a project, run your first briefMe call.",
		time: "~10 min",
	},
	{
		href: "./workflow/",
		label: "The session loop",
		desc: "The centerpiece workflow: briefMe → work → endSession → fresh chat.",
		time: "~5 min",
	},
	{
		href: "./why/",
		label: "Design rationale",
		desc: "Why local-first, why MCP-native, why the essential + catalog split.",
		time: "~7 min",
	},
	{
		href: "./anti-patterns/",
		label: "Anti-patterns",
		desc: "How people trip themselves up, and how to avoid it.",
		time: "~4 min",
	},
	{
		href: "./tools/",
		label: "MCP tools",
		desc: "Reference for every tool the agent can call.",
		time: "reference",
	},
]} />

</div>
