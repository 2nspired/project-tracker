import { ComingSoon } from "@/components/design-system/coming-soon";

export const metadata = { title: "Typography" };

export default function TypographyPage() {
	return (
		<ComingSoon
			title="Typography"
			teaser="Type scale, weights, and line heights — every font ramp the product uses, rendered live."
		/>
	);
}
