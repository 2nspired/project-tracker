import { ComingSoon } from "@/components/design-system/coming-soon";

export const metadata = { title: "Radius" };

export default function RadiusPage() {
	return (
		<ComingSoon
			title="Radius"
			teaser="Border-radius tokens (sm / md / lg / xl) and where each is appropriate."
		/>
	);
}
