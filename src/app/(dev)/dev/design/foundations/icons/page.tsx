import { ComingSoon } from "@/components/design-system/coming-soon";

export const metadata = { title: "Icons" };

export default function IconsPage() {
	return (
		<ComingSoon
			title="Icons"
			teaser="Pigeon's icon set (lucide-react), sized and stroked to match the product."
		/>
	);
}
