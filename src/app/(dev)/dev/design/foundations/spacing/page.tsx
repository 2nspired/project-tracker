import { ComingSoon } from "@/components/design-system/coming-soon";

export const metadata = { title: "Spacing" };

export default function SpacingPage() {
	return (
		<ComingSoon
			title="Spacing"
			teaser="The spacing scale used across the app, with worked examples for stack, inline, and inset patterns."
		/>
	);
}
