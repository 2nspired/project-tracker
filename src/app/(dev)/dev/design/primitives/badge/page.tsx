import { ComingSoon } from "@/components/design-system/coming-soon";

export const metadata = { title: "Badge" };

export default function BadgeShowcasePage() {
	return (
		<ComingSoon
			title="Badge"
			teaser="Badge variants used across status pills, tag chips, and counts."
		/>
	);
}
