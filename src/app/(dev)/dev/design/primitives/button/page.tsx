import { ComingSoon } from "@/components/design-system/coming-soon";

export const metadata = { title: "Button" };

export default function ButtonShowcasePage() {
	return (
		<ComingSoon
			title="Button"
			teaser="Every variant and size of `<Button />` rendered live, with copyable usage code."
		/>
	);
}
