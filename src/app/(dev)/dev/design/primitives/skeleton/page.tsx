import { ComingSoon } from "@/components/design-system/coming-soon";

export const metadata = { title: "Skeleton" };

export default function SkeletonShowcasePage() {
	return (
		<ComingSoon
			title="Skeleton"
			teaser="Loading skeletons paired with the real components they stand in for."
		/>
	);
}
