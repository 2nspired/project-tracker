import { ComingSoon } from "@/components/design-system/coming-soon";

export const metadata = { title: "Card" };

export default function CardShowcasePage() {
	return (
		<ComingSoon
			title="Card"
			teaser="Card composition (header / content / footer / action) with the spacing rules we follow."
		/>
	);
}
