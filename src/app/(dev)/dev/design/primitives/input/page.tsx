import { ComingSoon } from "@/components/design-system/coming-soon";

export const metadata = { title: "Input" };

export default function InputShowcasePage() {
	return (
		<ComingSoon
			title="Input"
			teaser="Form inputs, including states (default / focus / invalid / disabled) side-by-side."
		/>
	);
}
