import { ComingSoon } from "@/components/design-system/coming-soon";

export const metadata = { title: "Surfaces" };

export default function SurfacesPage() {
	return (
		<ComingSoon
			title="Surfaces"
			teaser="App-level surfaces — boards, sheets, dialogs — in their canonical compositions."
		/>
	);
}
