import { ComingSoon } from "@/components/design-system/coming-soon";

export const metadata = { title: "Step Section" };

export default function StepSectionPage() {
	return (
		<ComingSoon
			title="Step Section"
			teaser="The step-section pattern used in setup, onboarding, and multi-step flows."
		/>
	);
}
